# Passorder Wireframe (Vite + React + TS)

SPA migration of the previous static prototype. Run with `npm install && npm run dev`. Routes: `/` (landing), `/store`, `/menu`, `/magazine`. Build: `npm run build`. Deploy via Vercel (SPA rewrites already configured in `vercel.json`).
